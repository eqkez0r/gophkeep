version: "3"

tasks:
  build:
    - mkdir ./build
    - go build -o build/agent ./cmd/agent -ldflags  -ldflags="-X main.buildVersion=v0.0.1 -X main.buildDate=$(shell date -u +%Y-%m-%dT%H:%M:%S) -X main.buildCommit=$(shell git rev-parse HEAD)"
    - go build -o build/server ./cmd/server